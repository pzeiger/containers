Bootstrap: docker
From: ubuntu:24.04

%post
    # Generated for dardel24.11
    # Generated on: 2025-11-16T00:21:18.059017

    # Module: base_tools
    # Base development tools module
    apt-get update && apt-get full-upgrade -y && \
    apt-get install -y \
    build-essential \
    cmake \
    git \
    wget
    USER ubuntu
    WORKDIR /home/ubuntu

    # Module: python
    # Copy uv into image
    USER ubuntu
    COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /home/ubuntu/bin/
    ENV PATH="/home/ubuntu/bin:$PATH"
    ENV VIRTUAL_ENV=/opt/venv
    uv venv --python 3.12
    uv pip install --no-cache-dir --upgrade pip setuptools wheel
    ENV PATH="$VIRTUAL_ENV/bin:$PATH"


%environment